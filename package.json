{
  "name": "myrestapi",
  "version": "1.0.0",
  "description": "A simple REST API based on RESTIFY and MongoJS.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NESB",
  "license": "MIT",
  "devDependencies": {
    "mongojs": "^2.4.0",
    "restify": "^4.1.1"
  }
}
